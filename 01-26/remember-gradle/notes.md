gradle init --package=std

./gradlew run
./gradlew tasks - ver todas as tarefas do gradle

 ./gradlew installDist
/home/aluno/remember-gradle/aplicacao/app/build/install/app

# executar a minha aplicação
./bin/app

docker network ls
docker network create minhared
docker run -it --rm --network minhared alpine