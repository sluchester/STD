# Cliente e servidor com sockets tcp

Para executar via terminal (na raiz do diretório principal):
```cmd
./gradlew run
```

para argumento de linha de comando:
```cmd
./gradlew run --args "127.0.0.1 5555"
```